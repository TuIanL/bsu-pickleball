fastapi>=0.115.0
numpy>=1.26.0
opencv-python>=4.10.0
pandas>=2.2.0
pydantic>=2.8.0
python-multipart>=0.0.9
uvicorn[standard]>=0.30.0

# Development and tests
pytest>=8.0.0
httpx>=0.27.0

# Optional model-backed detection/tracking
ultralytics>=8.3.0

# Optional RTMPose validation/runtime
# Install these only in environments that will run true RTMPose inference.
# MMCV is intentionally installed via OpenMIM because wheels vary by platform,
# PyTorch, and CUDA version:
#   python -m pip install openmim
#   python -m mim install "mmcv>=2.0.1"
#   python -m pip install "mmpose>=1.3.0" "mmengine>=0.10.0" "torch>=2.1.0"
