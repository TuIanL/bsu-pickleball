# Backend Runtime Data

Local runtime artifacts live here during MVP development:

- `uploads/`: source videos uploaded through the API
- `outputs/`: analysis JSON results and visualized video artifacts
- `calibrations/`: manual court calibration JSON files
- `tmp/`: temporary frames and intermediate processing files

Generated media, model outputs, calibration JSON, and temporary files are ignored by git.
