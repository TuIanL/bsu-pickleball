# 2026-07-09 工作日志

## 诊断：job-4879e5beb4 骨架卡顿/不跟随移动
- 只读诊断，未修改任何代码。详细报告见 `backend/data/outputs/job-4879e5beb4/DIAGNOSIS_骨架卡顿.md`。
- 结论链：
  - 骨架数据本身正常（pose_overlay.json 内相邻采样帧 nose 位移 4.9–9.3px，RTMPose 没坏）。
  - 根因1（后端稀疏）：视频 10122 帧/60fps，stride=2 → 处理 5061 帧；但 `court_view_roi.json` 显示 `non_court_view_frame_count=4844`（gated 4844），仅 `court_view_frame_count=217` 进姿态估计（占视频 2.1%）。12 段短簇，最大空洞 2302 帧（≈38s），track1 最长连续空洞 4136 帧（≈69s）。
  - 根因2（前端冻结）：`src/components/platform/videoOverlayPlayback.ts` 的 `resolvePoseFrame`/`findFrameWindow` 在空洞区间取上一帧骨架作 current，按 track_id 匹配 next 插值；跨几十秒空洞 track_id 不匹配则原样返回→完全冻结，匹配到也因两端相隔几十秒而视觉静止。
- 关键配置：`overlay_frame_stride=2`、`enable_court_view_gate=True`（backend/app/core/config.py）。
- 修复方向（未执行）：调宽 court_view 阈值或对本任务关门控验证；前端对长空洞改 fade-out/隐藏而非沿用上一帧。
- [openspec-explore 补充复查] 读取实际产物数据精确验证了 track_id 跨段连续性：track1 仅出现于簇1和簇12（间隔 163s），track2 跨簇1-4但有 24-57s 间隔，track3 在簇1和簇4出现后消失→空洞内 track_id 无交集时骨架完全冻结。15 个骨架簇，平均每簇 ~0.7s 数据，簇间空洞 5.7–76.7s。追加了三张可视化时序图到诊断回复中（帧分布漏斗/前端冻结机制/覆盖时间线）。
