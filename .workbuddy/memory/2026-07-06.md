# 2026-07-06

## 为 backend/app/api 下 7 个文件添加详细中文注释
- 文件：`__init__.py`、`analysis.py`、`routes_video.py`、`routes_calibration.py`、`routes_analysis.py`、`routes_camera.py`、`routes_recording.py`
- 用户说明自己是编程小白，因此注释重点解释 FastAPI 概念（路由、请求/响应、HTTP 状态码、Schema、HTTPException、装饰器等）及每个接口的业务含义。
- 注释方式：保留原有逻辑，仅补充模块 docstring、import 说明、每个路由函数的 docstring 与关键行内注释。
- 验证：`python3 -m py_compile` 7 个文件，语法全部通过。

## 为 backend/app/camera 下 6 个文件添加详细中文注释
- 文件：`__init__.py`、`models.py`、`camera_registry.py`、`stream_probe.py`、`recorder.py`、`session_service.py`
- 用户明确要求"不要改任何业务逻辑和代码运行行为"，因此严格只插入 docstring 与 # 注释，未改动任何代码行（包括 models.py 字段、session_service.py 中冗余的 suffix 逻辑、recorder.py 的 ffmpeg 命令行参数均保持原样）。
- 注释重点：摄像头"登记→探测→录制→会话生命周期"链路；ffmpeg 各参数含义；全局单例与内存缓存设计；异步探测线程池。
- 验证：`python3 -m py_compile` 6 个文件，语法全部通过。

## 为 backend/app/core 下 3 个文件添加详细中文注释
- 文件：`__init__.py`、`logging.py`、`config.py`
- 要求与之前一致：只加注释，不改业务逻辑与运行行为。config.py 的 Settings 字段按主题分组加尾注（应用信息、目录、检测模型、姿态、主球员、球员身份、场地线、任务队列、发球、场地视角、ROI），get_settings 函数加整体说明 + 关键行内注释（PICKLEBALL_ 环境变量覆盖机制、rtmpose 路径查找、_env_bool/_clamp_float 等辅助函数作用），所有默认值/代码行保持原样。
- 验证：`python3 -m py_compile` 3 个文件，语法全部通过。

## 为 backend/app/schemas 下 11 个文件添加详细中文注释
- 文件：`__init__.py`、`video.py`、`metrics.py`、`multitarget.py`、`calibration.py`、`pose.py`、`court_view.py`、`events.py`、`pipeline.py`、`analysis.py`、`tracking.py`
- 要求一致：仅加中文注释，不改变任何逻辑、字段、默认值、field_validator/model_validator 行为。
- 注释重点（面向小白）：Pydantic BaseModel/Field/Literal/Optional 概念；每个 Schema 的业务含义（视频元数据、运动指标、多目标检测、标定与单应矩阵、姿态骨架、球场视角 ROI、发球事件、流水线阶段、分析报表、球员跟踪轨迹与脚点投影）；对 `_validate_point`/`_validate_bbox` 等校验函数、FootpointMethod/CourtUnit 等 Literal 别名加了用途说明。
- 验证：`python3 -m py_compile` 全部 11 个文件，语法全部通过。至此 api/camera/core/schemas 四个子目录的注释任务均已完成。

## 为 backend/app/services 下 9 个文件添加详细中文注释
- 文件：`__init__.py`、`storage_service.py`、`video_service.py`、`calibration_service.py`、`automatic_calibration_service.py`、`analysis_pipeline.py`、`job_orchestration.py`、`mock_analysis.py`
- 要求一致：仅加中文注释（模块/docstring、import 说明、函数与类职责、关键逻辑行内注释），未改动任何业务逻辑、字段、默认值、参数或运行行为。
- 注释重点（面向小白）：存储服务各产物路径构造器与原子写；视频上传流式分块写；标定单应矩阵计算/投影/预览；自动标定"分割→掩码转角点→置信度组合→预览"流程；分析流水线"检测→跟踪→投影→指标"主干与三条路径（真实/有限/demo）；任务编排 JobStore/Worker/取消令牌/重试/超时；mock 服务的任务 CRUD 与 demo 报告拼接。
- 验证：`python3 -m py_compile` 全部 9 个文件，语法全部通过。
- 至此 api/camera/core/schemas/services 五个子目录的注释任务均已完成。
