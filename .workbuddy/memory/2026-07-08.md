# 2026-07-08 工作日志

## 网络摄像头地址排查（环境/抓取设备）
- 用户通过网线接了一台网络摄像头（用于匹克球视频采集），忘记其 IP。
- 本机网络：`en0` = Wi-Fi 172.18.211.110/20（校园网，网关 172.18.208.1）；`en8` = 有线 192.168.1.200/24（网线直连摄像头所在网段）。
- 现象：连 Wi-Fi 时网线读不到摄像头；断开 Wi-Fi 后摄像头回应 ARP。
- 诊断：摄像头为 Wi-Fi+网口双模。有 Wi-Fi 时切无线、有线 PHY 休眠；断 Wi-Fi 才回落到有线出厂静态 IP（192.168.1.x）。校园 Wi-Fi 大概率有客户端隔离，即使走无线也跨客户端不可达。
- 验证手段（Wi-Fi 开着时均失败，符合预期）：ping 扫描 192.168.1.0/24 全 incomplete；ONVIF WS-Discovery 无响应；mDNS(_rtsp/_http) 无响应；172.18.208.0/20 内无摄像头类设备。
- 解法：关 Wi-Fi → 用 `find-camera.sh` 扫出摄像头 192.168.1.x IP → 进其 Web 管理页关闭/忘记 Wi-Fi（仅用有线）→ 重开 Wi-Fi。此后摄像头常驻 192.168.1.x，Mac 可同时看摄像头+上网。
- 产物：`find-camera.sh`（项目根，离线可用，ping 扫描 192.168.1.x 并自动开浏览器）。

## pose-overlay-export 归档
- 将验证任务 6.1/6.2 标记为完成，全部 13 个 tasks done
- 将 delta spec `swing-skeleton-export` 同步到 `openspec/specs/swing-skeleton-export/spec.md`（新建能力）
- change 归档到 `openspec/changes/archive/2026-07-08-pose-overlay-export/`
- 归档后剩余 1 个活跃 change：`add-session-timeline-manual-events`

## OpenSpec 概览
- 当前规格：spec-driven 模式，39 个能力 spec，0 个活跃 change，41 个已归档 change

## add-session-timeline-manual-events 实施完成（30/30 tasks）
- **后端新增文件**：`backend/app/models/timeline_event.py`（ORM 模型 + TimelineEventType/Source 枚举）、`backend/app/schemas/timeline_event.py`（Pydantic schema + 全字段校验）、`backend/app/services/timeline_event_service.py`（CRUD + 时间戳策略）、`backend/app/api/routes_timeline_events.py`（REST API + router）
- **后端修改**：`database.py`（注册新模型）、`main.py`（注册 router）、`routes_field_sessions.py`（删除保护：先检查 timeline events 再 db commit，避免时序 bug）
- **前端新增文件**：`src/services/timelineQuickEvents.ts`（快捷事件映射：match/practice/engineering 三模式）+ 配套 `timelineQuickEvents.test.ts`
- **前端修改**：`src/types/report.ts`（新增 6 个类型）、`src/services/analysisClient.ts`（新增 5 个 API 函数）、`src/App.tsx`（CameraHubPage 增加 timeline event 状态、加载逻辑、快捷打点面板、事件列表、编辑 Modal）
- **测试**：后端 22 个 pytest 全部通过、前端 5 个 vitest 全部通过、tsc 类型检查通过
- **修复的 bug**：删除保护时序错误 — 原逻辑先 `delete_field_session()` db.commit() 再检查 timeline events 计数，导致 409 响应后 FS 已被删除；改为先检查后删除

## job-6161f4422b 卡死故障（确认 --reload 缺陷再次发生）
- 任务状态文件 `backend/data/outputs/jobs/job-6161f4422b.json` 显示 `processing / frame-sampling / 34%`，但 mtime 为 08:06:14（创建后从未更新）→ 滞后快照。
- 真实进度在 `backend.log`：player tracking 推进到 6510/71976 帧（≈9%），最后一行时间戳 08:55:15。
- 08:55 起日志出现一连串 `WatchFiles detected changes ... Reloading...`（触发源：编辑 `app/models/timeline_event.py`、`app/database.py`、`app/main.py`、`app/api/routes_field_sessions.py` 等源码），uvicorn --reload 重启把正在跑的分析 worker 线程杀掉。
- 截至 09:09 已 14 分钟无任何 progress 行，日志只剩前端轮询 GET 200 → 任务已孤儿化（server 进程存活，但无线程执行该 job）。
- 根因：`scripts/start-local-runtime.sh:214` 用 `uvicorn ... --reload`，长任务期间编辑源码即触发重启杀线程（与 MEMORY 中记录的缺陷一致，但本次触发源是源码编辑而非 data 写入，故 `--reload-exclude backend/data` 不足以防范，必须禁用 --reload 或把分析改为独立常驻进程 + 启动自愈）。
- 待办：与用户确认是否修复启动脚本（禁用 --reload / 分析进程独立化）+ 重提该 job。

## 录制跳帧故障排查与修复
- 问题：录制功能能正常录，但回放时每隔几秒卡 1-2 秒（帧被跳过），实时预览正常。
- 根因：`backend/app/camera/recorder.py` 中 FFmpeg 命令行参数冲突——`-c:v copy`（流复制模式）与 `-r`、`-s`、`-fps_mode cfr` 不可同时使用。复制模式下这些参数不生效，反而导致 FFmpeg 通过篡改 PTS 时间戳来"凑"帧率，输出 MP4 时间轴不规则，浏览器播放时卡顿。
- 预览不受影响：预览走独立的 `cv2.VideoCapture` 路径，与 FFmpeg 无关。
- 修复：
  - 从 `recorder.py` 的 FFmpeg 命令中移除 `-r`、`-s`、`-fps_mode cfr`，仅保留 `-c:v copy` + `-c:a aac`，让 FFmpeg 原样复制源流。
  - 清理 `Recorder.start()` 方法签名，移除已无实际效用的 `fps` 和 `resolution` 参数。
  - 同步清理 `session_service.py` 调用处，不再传 `fps` 和 `resolution`（它们作为 request/session 元数据字段保留）。
- 二次排查发现修复后仍然卡顿：分析 `rec_20260708_012732.mp4` 帧 PTS，发现 4.022s-7.989s 存在 3.97s 大缺口（361 帧丢失）。
- 根因升级：预览模块（`preview_service.py`）用独立 `cv2.VideoCapture` 连接摄像头，加上 FFmpeg 录制连接，共 2 路并发 RTSP → 摄像头 RTSP 服务过载丢帧。
- 修复：前端 `App.tsx` 中 `handleStartRecording` 录制前自动断开预览（`setPreviewStatus("idle")` + key 递增），`handleStopRecording`/`handleCancelRecording` 停止后自动恢复预览。录制期间 UI 显示"录制期间预览已暂停"提示。

## OpenSpec 提案：redesign-product-entry-and-capture-console
- 通过 openspec-explore 模式深入分析了现有前端代码（App.tsx 5000+ 行、AppShell 导航、CameraHubPage、类型系统、API 客户端）
- 验证结论：所有 Field Session、Recording、Camera、TimelineEvent 类型和 API 已就绪，本次纯前端重构无需任何后端改动
- 创建了完整的 change proposal（proposal/design/specs/tasks 共 12 组 57 个任务）
- 核心改动：移除 4 tab 导航 → 极简导航；首页改为双工作流入口（上传模式/录制模式）；录制模式拆为 4 状态工作流（首页→向导→控制台→完成面板）；摄像头管理收入设备抽屉；训练页软隐藏
- 采用「轻拆策略」：仅拆 6 个页面级组件到 src/pages/，不拆子组件
- change 位于 openspec/changes/redesign-product-entry-and-capture-console/

## OpenSpec 实施：redesign-product-entry-and-capture-console 完成
- **全部 78 个任务完成**（12 组，含 proposal 修正后的 7 个补充任务）
- **修改文件**：
  - `src/types/report.ts`: 扩展 AppPath 类型，新增 /upload, /capture, /capture/new, /capture/:id, /tasks
  - `src/App.tsx`: RouteState 改为 name 联合类型；parsePath 新增 7 个路由解析；navigate 支持 query params；路由渲染用新页面组件；移除 OverviewPage
  - `src/components/platform/AppShell.tsx`: 完全重写 — 移除 tab pill 导航，改为 Logo左 + 任务历史右；移除 navigation prop
  - `src/data/demoData.ts`: platformNavigation 精简为仅首页
- **新增文件**（src/pages/）：
  - `LandingPage.tsx`: Hero + 双入口按钮 + 3 能力卡片
  - `UploadModePage.tsx`: 占位薄 wrapper（实际通过 App.tsx switch 渲染 NewAnalysisPage）
  - `CaptureHomePage.tsx`: 采集任务列表 + 新建入口 + 空状态
  - `CaptureWizardPage.tsx`: 三步向导（采集场景/摄像头方案/分析设置）+ sessionStorage 兜底
  - `CaptureConsolePage.tsx`: 左预览 + 右控制 + 事件标记 + 时间线 + 完成面板 + 设备抽屉
  - `TasksPage.tsx`: 占位薄 wrapper
- **构建验证**: tsc --noEmit 零错误，vite build 成功（441KB JS + 55KB CSS）
- **保留兼容**: /camera, /training, /analysis/* 旧路由继续可用；CameraHubPage 组件未删除
