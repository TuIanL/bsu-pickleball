## 1. 数据模型与纯计算层

- [x] 1.1 梳理 `PipelineTrackPoint`、`BallTrajectoryArtifact`、`BounceEventsArtifact` 和现有视频时间同步逻辑，确定 HUD 所需的最小输入与缺失状态映射
- [x] 1.2 新增视频 HUD 纯计算模块，按当前时间窗口生成球员轨迹段、球轨迹段、当前位置和弹跳候选，支持非法坐标、空数组和时间缺口
- [x] 1.3 在纯计算模块中实现稳定降采样、轨迹断线、球员方向计算和单位可确认时的速度摘要，禁止跨缺口连接数据
- [x] 1.4 为时间窗口、断线、插值点、低置信度点、越界点、未知单位和无数据状态补充单元测试

## 2. 球场小地图 HUD

- [x] 2.1 重构 `CourtMinimap` 的 viewBox 和坐标变换，保持标准球场纵横比并将正式球场作为主视觉区域
- [x] 2.2 增加球员颜色、P1-P4 身份标签、当前位置光晕、方向箭头、时间尾迹、球网、厨房区和简洁图例
- [x] 2.3 将球轨迹平面投影、当前球点、弹跳候选、插值段和低置信度状态接入小地图渲染
- [x] 2.4 增加 HUD 信息摘要，包括当前时间、可见球员数量、球层状态和可用的速度/置信度信息
- [x] 2.5 为正式球场、tracking bounds、越界点和小地图空状态补充组件测试，确认不出现比例变形或原始 track_id 泄漏

## 3. 视频叠加与图层控制

- [x] 3.1 扩展 `VideoAnalysisCard` 和 `VisionPage` 的 props 传递，使小地图同时接收球轨迹和弹跳候选 artifact
- [x] 3.2 重做视频主画面球轨迹渲染，使用基于时间的渐隐尾迹、断线规则、当前球点和插值/检测视觉区分
- [x] 3.3 将人框、骨架、球点、球路和弹跳候选改为独立图层状态，并为不可用图层提供禁用原因和辅助标签
- [x] 3.4 在视频 HUD 中增加二维图像球路、球场平面投影和视觉估算说明，避免将二维数据描述为真实三维高度或球速
- [x] 3.5 保持现有播放、拖动、发球候选跳转、音量和全屏逻辑不变，并验证图层切换不改变播放位置

## 4. 响应式、状态和可访问性

- [x] 4.1 为桌面、窄屏和全屏模式设置稳定的 HUD 尺寸、位置和 z-index，确保不遮挡播放控件或视频主体
- [x] 4.2 完善 loading、failed、unavailable、no-candidates 和当前时间无有效点的 HUD 状态，不回退到 demo 球员或静态球路
- [x] 4.3 为独立图层控制补充 `aria-pressed`、`aria-label`、键盘焦点样式和 tooltip/辅助说明
- [x] 4.4 对真实任务样例检查 4 名球员、球轨迹缺口、130 个弹跳候选和越界投影的渲染表现，确认状态文案准确

## 5. 验证与回归

- [x] 5.1 更新 `CourtMinimap`、`VideoAnalysisCard` 及 HUD 纯计算相关测试，覆盖真实 artifact 和无数据 fixture
- [x] 5.2 运行前端 typecheck、lint 和相关 Vitest 测试，修复组件 props、SVG geometry 和时间同步回归
- [x] 5.3 使用真实分析任务打开视觉分析页，检查播放、拖动、全屏、各图层开关、地图比例和球路可见性
- [x] 5.4 在桌面和窄屏 viewport 截图核对 HUD 不重叠、不变形，记录仍受二维高度数据限制的视觉估算提示
