## 1. 合并状态与持久化模型

- [x] 1.1 为 `SyncRecordingSession` 增加任务级和机位级合并状态、错误信息及结果字段，并为旧会话实现缺失字段的兼容默认值
- [x] 1.2 设计并实现合并状态转换与持久化辅助逻辑，覆盖待合并、合并中、已完成、失败和重复提交场景
- [x] 1.3 增加双摄任务两路 TS 文件发现与有效性校验，确保合并前不修改或删除原始 TS

## 2. 后端停止流程与合并 API

- [x] 2.1 修改双摄正常停止流程，移除停止请求中的 `_register_session_videos()` MP4 合并和 Video 登记调用
- [x] 2.2 保留 TS 分段收尾、CaptureTake 终态化、capture manifest 和时间线元数据持久化，并将会话标记为待合并
- [x] 2.3 实现任务级合并服务：按 cam_1、cam_2 依次调用现有 Finalizer，持久化每路结果，并在两路成功后登记默认分析视频
- [x] 2.4 增加 `POST /api/sync-recordings/{session_id}/merge` 显式合并接口，校验任务状态、原始文件和重复请求
- [x] 2.5 为后台合并增加并发保护、异常捕获和失败重试逻辑，服务重启后不得将遗留的“合并中”任务误报为已完成
- [x] 2.6 保持单摄 `SessionService.stop_session()` 的直接 MP4 登记和自动分析行为不变

## 3. 前端任务管理

- [x] 3.1 在 `analysisClient.ts` 增加双摄合并 API，并扩展 `SyncRecordingSession` 类型以表达合并状态
- [x] 3.2 更新 `AnalysisTasksPage` 的双摄任务加载和刷新逻辑，使合并提交后能显示最新状态
- [x] 3.3 更新 `SyncRecordingTaskCard`：待合并显示“合并视频”，合并中禁用重复提交，失败显示原因和“重新合并”
- [x] 3.4 在两路合并完成且存在有效 Video ID 前隐藏播放、工作台视频和分析入口；完成后恢复现有入口
- [x] 3.5 为合并失败、部分机位完成、无 TS 文件和任务删除冲突补充明确的用户反馈

## 4. 自动化测试与验证

- [x] 4.1 增加后端测试，验证双摄 stop 只保存 TS，不调用 MP4 合并和 Video 登记
- [x] 4.2 增加后端测试，验证任务级合并会依次处理两路、两路完成后才可播放/分析、失败保留 TS 且可重试
- [x] 4.3 增加后端测试，验证重复合并请求、已完成幂等复用、服务重启后的状态恢复和删除并发保护
- [x] 4.4 增加前端组件测试，验证任务卡片各合并状态下的按钮、状态文案及播放/分析入口
- [x] 4.5 运行 TypeScript、Python 测试和 OpenSpec 校验，并使用短录双摄完成停止快速返回、逐任务合并和失败重试的端到端验证
