## 1. 阶段图与状态机基础

- [x] 1.1 在后端集中定义 `single_view`、`late_fusion_v1`、`joint_tracking_v2` 的阶段图、阶段 ID、label 和阶段权重，并保留既有单摄阶段顺序
- [x] 1.2 实现统一的阶段状态转换器，校验 active/pending/终态约束、阶段进度范围、非法阶段 ID、失败和取消终态
- [x] 1.3 实现按阶段权重聚合总体进度的纯函数，支持当前阶段进度、A/B 子进度、单调不回退和成功 100% 规则
- [x] 1.4 为阶段图、状态转换和进度聚合补充后端单元测试，覆盖报告顺序、`multiview-joint=95`、延迟旧遥测和失败/取消场景

## 2. 后端任务编排接入

- [x] 2.1 将 `JobStore` 的创建、认领、阶段更新和终态更新切换到模式化阶段状态机，移除把双摄阶段追加到单摄列表末尾的路径
- [x] 2.2 将 `late_fusion_v1` 的 child 阶段和 `MultiViewFusionRun` 接入 Parent 聚合图，确保 A/B、融合、指标、可视化、报告按顺序推进
- [x] 2.3 将 `joint_tracking_v2` 执行器的进度回调接入 `multiview-joint` 阶段，并同步内部 A/B `ViewRun` 的实时遥测
- [x] 2.4 更新双摄 Coordinator 和结果合成器，使运行中与终态使用同一份规范化阶段快照，避免合成器重新生成另一套阶段列表
- [x] 2.5 增加编排集成测试，覆盖两种双摄模式的阶段推进、Parent/child 状态关系、报告只能在后处理之后开始以及总体进度单调性

## 3. API 契约与历史兼容

- [x] 3.1 更新 `AnalysisJobSummary` 和相关 schema/序列化逻辑，返回按阶段图排序的 `stages`、当前 `stage`、阶段详情和规范化总体进度
- [x] 3.2 统一 `viewRuns` 的来源和返回规则：late fusion 使用 dedicated child，joint tracking 使用内部 A/B `ViewRun`，无真实子运行时省略或明确表示未开始
- [x] 3.3 为缺少新字段的历史单摄和双摄任务实现只读兼容归一化，确保不修改历史持久化数据且不会因缺字段返回 500
- [x] 3.4 补充 API schema/路由测试，验证字段缺省、阶段顺序、失败/取消错误信息、`viewRuns` 非空语义和成功 100% 语义

## 4. 前端进度页面

- [x] 4.1 更新 `AnalysisJobPage` 和 `JobStageStepper`，使用 API 返回的阶段顺序和状态渲染，不再套用单摄固定阶段数组
- [x] 4.2 为双摄任务增加与顶层阶段一致的 A/B 子进度展示，并在 `viewRuns` 缺失或为空时隐藏子进度区域
- [x] 4.3 调整运行中、失败、取消和完成状态的文案、高亮和总体进度显示，确保报告不会提前显示为可进行阶段
- [x] 4.4 更新前端 API 类型、辅助函数和测试，覆盖 joint tracking 协同跟踪 95%、late fusion A/B child、历史任务和空 `viewRuns`

## 5. 端到端验收与清理

- [ ] 5.1 使用真实双摄分析任务验证后端日志、任务 API 快照和页面阶段顺序一致，确认读取视频后不会直接跳到错误的终态阶段关系
- [x] 5.2 验证 `single_view` 历史和新建任务的阶段、进度与现有行为兼容，并确认新增双摄逻辑不改变视觉分析算法输出
- [x] 5.3 运行后端和前端完整相关测试，修复阶段 ID、进度回退、报告提前激活和 A/B 遥测为空的回归
- [x] 5.4 删除仅用于临时拼接双摄阶段的旧分支和重复聚合逻辑，更新必要的类型注释与开发文档
