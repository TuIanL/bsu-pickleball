## 1. Runtime Commands

- [x] 1.1 Add a startup script that launches the RTMPose-enabled backend and frontend with repository-local defaults.
- [x] 1.2 Add a shutdown script that stops recorded startup processes and clears stale PID files.
- [x] 1.3 Add convenient package scripts and macOS command wrappers for one-command use.

## 2. Documentation and Verification

- [x] 2.1 Document the one-command start and stop workflow, logs, and RTMPose checkpoint loading assumption.
- [x] 2.2 Verify startup preflight behavior and shutdown behavior without requiring a full long-running manual session.
