## 1. 已保存候选与抽样数据

- [x] 1.1 扩展评分校准候选适配器，从 CaptureTake 的 `session_dir/analysis/job-*` 读取发球及可用 shot/rally 候选，并按 registered video ID 过滤。
- [x] 1.2 为落盘候选补充 job、artifact、detector version、coverage warning 等 provenance 字段，并保持内存 `mock_analysis.JOBS` 兼容。
- [x] 1.3 基于现有有效 rally 片段实现默认 12 条均匀抽样、全部回合切换和回合进度计算，不新增数据库迁移。
- [x] 1.4 为候选扫描、视频 ID 过滤、抽样边界和候选不可用原因补充后端单元测试。

## 2. 快速校准交互

- [x] 2.1 增加快速模式的最小事实动作和队列进度 TypeScript 类型，复用现有标注 API payload。
- [x] 2.2 将工作台默认视图改为回合抽样队列，打开当前 rally 的视频窗口并显示已处理/待处理/跳过进度。
- [x] 2.3 接入落盘候选到当前回合时间轴和候选区，显示来源与覆盖提示；无候选时展示可解释的空状态。
- [x] 2.4 增加“发球入界、发球失败、接发入界、接发不可观察、跳过”快捷操作，并自动填充事件时间、证据窗口、回合和语义字段。
- [x] 2.5 实现快捷保存后自动进入下一条、重复点击保护和当前回合状态反馈，不绕过现有校验及 revision 语义。
- [x] 2.6 将详细表单收纳为高级信息区，允许补充击球人、落点、置信度和备注，并保留时间线/队列进入详细编辑的入口。
- [x] 2.7 支持快速模式下的 draft/reviewed/locked 展示、Gold Set 锁定和 locked revision 只读行为。

## 3. 测试与使用说明

- [x] 3.1 为抽样队列、快捷语义、不可观察接发、保存下一条、跳过和高级字段补充增加前端测试。
- [x] 3.2 使用 `ct_6949bef776a5` 的真实本地视频完成快速模式端到端验收，确认候选/无候选两种状态均可继续人工校准。
- [x] 3.3 确认既有详细标注、片段管理、Gold Set API 和报告行为不受影响。
- [x] 3.4 更新工作台使用说明，明确第一批只需抽样 12 个回合，不要求人工重做整场比赛。
