## 1. 后端模型与服务骨架

- [ ] 1.1 新增双摄同步录制 Pydantic 模型，覆盖开始请求、停止响应、机位槽位、分段、短录测试结果和会话状态
- [ ] 1.2 新增双摄同步录制会话持久化目录和读写工具，避免影响现有单摄 `RecordingSession` JSON 格式
- [ ] 1.3 新增摄像头占用检查工具，统一判断摄像头是否被单摄或双摄录制占用
- [ ] 1.4 扩展现有单摄开始录制逻辑，使其拒绝使用正在参与双摄同步录制的摄像头

## 2. 同步录制核心

- [ ] 2.1 从独立脚本提炼 `SyncRecorder`，支持同时启动两个 FFmpeg 进程并写入同编号 `.ts` 分段
- [ ] 2.2 实现停止事件、失败事件、分段编号和录制线程管理
- [ ] 2.3 实现任一路 FFmpeg 异常退出后终止所有路并同步重启下一分段
- [ ] 2.4 实现用户停止录制时终止所有进程、等待线程退出并生成 completed 会话
- [ ] 2.5 实现测试分段的首帧和尾帧提取，复用 OpenCV 并返回可展示路径
- [ ] 2.6 记录每个分段的路径、状态、错误摘要、开始/结束时间和重启次数

## 3. 双摄录制 API

- [ ] 3.1 新增双摄同步录制路由并挂载到 FastAPI 应用
- [ ] 3.2 实现开始双摄同步录制 API，校验两个不同摄像头、Field Session 和 FFmpeg 可用性
- [ ] 3.3 实现停止双摄同步录制 API，返回最终会话状态和输出摘要
- [ ] 3.4 实现查询双摄同步录制会话 API，支持活跃和已完成会话
- [ ] 3.5 实现双摄短录测试 API，不创建正式录制会话
- [ ] 3.6 在摄像头删除逻辑中阻止删除正在参与双摄录制的摄像头

## 4. 主机位视频登记与分析入口

- [ ] 4.1 确认现有 VideoService 对 `.ts` 的支持边界，并选择主机位登记策略
- [ ] 4.2 若需要，新增主机位分段合并或转码为 MP4 的后处理
- [ ] 4.3 双摄录制完成后登记主机位视频引用，写入 `primary_video_id` 或等价字段
- [ ] 4.4 在登记失败时保留双摄文件路径和错误信息，并让 API 明确返回分析入口不可用

## 5. 前端 API 与类型

- [ ] 5.1 在 `src/types/report.ts` 中新增双摄同步录制请求、会话、分段和测试结果类型
- [ ] 5.2 在 `analysisClient` 中新增开始、停止、查询和短录测试函数
- [ ] 5.3 为双摄控制台状态新增机位槽位、测试结果和同步录制状态类型

## 6. 采集控制台双摄 UI

- [ ] 6.1 在 `CaptureConsolePage` 中根据 `fieldSession.camera_setup === "dual"` 切换双摄控制台路径
- [ ] 6.2 实现主机位和副机位槽位卡片，展示摄像头名称、连接状态、预览入口和更换操作
- [ ] 6.3 将设备抽屉选择逻辑扩展为按槽位选择，并阻止两个槽位选择同一摄像头
- [ ] 6.4 使用 `sessionStorage` 持久化并恢复当前 Field Session 的双摄槽位选择
- [ ] 6.5 实现双摄短录测试按钮和测试结果展示，包括首帧、尾帧和错误信息
- [ ] 6.6 实现双摄同步开始、停止、录制中状态和完成面板
- [ ] 6.7 双摄录制中展示时长、当前分段编号、已保存分段数、重启次数和最近错误
- [ ] 6.8 双摄完成面板在主机位可分析时提供创建分析任务入口，不可分析时展示原因

## 7. 测试与验证

- [ ] 7.1 为 `SyncRecorder` 添加单元测试，覆盖正常分段、异常重启、用户停止和进程清理
- [ ] 7.2 为双摄 API 添加测试，覆盖开始、停止、查询、短录测试、摄像头缺失、重复摄像头和占用冲突
- [ ] 7.3 为单摄录制占用保护添加回归测试，确认双摄占用时单摄开始失败
- [ ] 7.4 为设备抽屉槽位选择和重复选择保护添加前端测试
- [ ] 7.5 为双摄控制台录制状态切换、短录测试结果和完成面板添加前端测试
- [ ] 7.6 运行后端测试、前端测试和类型检查，记录任何无法在本地验证的硬件相关项目

## 8. 文档与现场使用说明

- [ ] 8.1 在后端或项目文档中说明双摄同步录制所需 FFmpeg、摄像头 RTSP 地址和输出目录
- [ ] 8.2 记录双摄现场推荐流程：注册摄像头、探测、短录测试、开始同步录制、停止和创建分析
- [ ] 8.3 标注当前限制：同步控制不是帧级硬同步，双视角联合分析不在本次范围内
