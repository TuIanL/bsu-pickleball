## 1. 第一阶段：建立隔离验证基线

- [x] 1.1 记录当前 git diff、未提交候选复核实现和审核命令结果，确定本变更文件边界，不覆盖既有工作。
- [x] 1.2 统一 pytest 根目录/backend 两种入口的模块解析，验证收集集合一致且不忽略失败文件。
- [x] 1.3 修复 StubLockManager 的 slots 契约并保留正式检测/候选检测互斥断言，单独及全量运行均通过。
- [x] 1.4 完善 Settings 和测试隔离，覆盖摄像头、候选、控制面、静态测试帧及单例；验证测试不读写真实运行目录。

## 2. 第一阶段：摄像头路径与凭据

- [x] 2.1 在 schema/服务层共用安全 ID 验证并使用配置根目录，覆盖相对越界、绝对路径、反斜杠、控制字符、安全中文 ID 和不同 cwd。
- [x] 2.2 校验解析后目录归属并实现创建不覆盖，验证越界符号链接与并发同名注册；改名和删除保留录制占用限制。
- [x] 2.3 统一 API/错误/日志的独立密码和 URL userinfo 脱敏，用虚构凭据验证公开输出不泄露而内部连接参数完整。
- [x] 2.4 实现历史配置 dry-run/apply 迁移及清单，在副本上验证相同内容幂等、冲突不覆盖、非法 ID 不改名、源文件与录制引用保留。
- [x] 2.5 更新摄像头前端校验和错误提示，验证虚拟摄像头及设备抽屉既有行为。

## 3. 第一阶段：候选版本与事务真源

- [x] 3.1 增加 artifact 快照、复核版本和决定数据模型及迁移，建立 request_id 与候选决定唯一约束，验证新库/旧库升级。
- [x] 3.2 实现单次读取、schema/take/边界/唯一 ID 校验、原始 bytes SHA-256 与不可变快照；验证文件重生成不继承旧决定。
- [x] 3.3 将决定、revision 条件更新、编辑操作、正式片段及 ordinal 变更放入同一事务，保留重叠与人工边界门控。
- [x] 3.4 加入 artifact_version/request_id 请求契约、重复请求结果回放与稳定冲突错误，验证同 ID 异内容、过期版本/revision 和已复核候选。
- [x] 3.5 使用真实 SQLite 独立连接验证竞争请求；注入提交失败确认决定、片段、操作和 revision 全部回滚。
- [x] 3.6 将 reviews JSON 改为可重建导出，唯一临时文件原子写入；验证导出失败后 API 仍返回已提交决定且重试无重复片段。
- [x] 3.7 实现旧 reviews 幂等导入与 legacy_unbound 审计，在副本上验证断裂引用、缺版本、冲突和重复迁移均不补造或删除片段。
- [x] 3.8 更新前端类型/API/复核页面以持有 artifact_version 和稳定 request_id；验证网络结果不明重试、409 刷新与草稿保留但不自动提交。
- [x] 3.9 完成接受/修正/拒绝的端到端 API 回归，验证新旧版本共存、错场次/损坏 artifact 降级、正式比分和训练门控保持不变。

## 4. 第一阶段：恢复视频播放与阶段验收

- [x] 4.1 按最终流路径初始化完整 MIME 映射，保留 faststart/merged 优先级，覆盖全部支持上传格式和缺失文件。
- [x] 4.2 提取并验证单范围解析：闭区间、开放区间、后缀、超长后缀、空文件、不可满足范围、非法输入与多范围策略。
- [x] 4.3 增加 HEAD 并验证状态/响应头/空正文，回归流连接中断后的文件句柄释放。
- [x] 4.4 在生成的小型可播媒体上验证播放、拖动和不支持编码提示，区分 HTTP 传输成功与浏览器解码成功。
- [x] 4.5 执行第一阶段前后端完整基础测试、build/lint 和 OpenSpec 校验；记录迁移备份/回滚演练结果后进入第二阶段。

## 5. 第二阶段：分析查询热路径与诊断开关

- [x] 5.1 将 legacy 导入移到启动迁移及显式重导命令，持久化完成标记；验证并发启动、坏文件、重复导入与已有控制面不覆盖。
- [x] 5.2 移除 get/list 的历史扫描和快照写入，增加直接控制面取消查询；兼容快照仅在状态变更后刷新。
- [x] 5.3 在 1/100/1000 个历史任务下验证固定查询数且零 JSON 扫描/读写，回归跨进程取消、heartbeat、lease 及终态恢复。
- [x] 5.4 接通 projection JSONL/overlay 独立开关，关闭时不初始化相应 writer/minimap；按现有 projection-diagnostics 规格验证四种开关组合和异常释放。

## 6. 第二阶段：上传资源与封面执行

- [x] 6.1 增加正整数上传上限配置及 multipart 入站累计限制，默认 20 GiB + 1 MiB 开销预算；验证未知/伪造长度请求在限制处终止。
- [x] 6.2 实现临时落盘和登记失败清理，覆盖 413、空文件、连接中断、磁盘/元数据故障，保证失败素材不进入目录。
- [x] 6.3 将封面生成放入受限线程执行器并保持超时降级，使用屏障证明抽帧等待期间 API 仍可响应。
- [x] 6.4 完成上传—登记—封面缺失回退回归，记录操作计数及资源行为证据，通过第二阶段质量检查。

## 7. 第三阶段：依赖、启动和持续集成

- [x] 7.1 以 backend/pyproject.toml 收敛正式依赖和可选分组，补齐 SQLAlchemy 等入口依赖并消除根 Python 版本冲突。
- [x] 7.2 生成基础 Python 3.11 锁定安装产物和 requirements，记录生成方式/平台；分别在干净环境验证包安装与 requirements 安装。
- [x] 7.3 实现默认无模型基础启动和显式模型预检，确保 API/Worker 配置一致，保留端口冲突、启动失败清理与优雅停止。
- [x] 7.4 为可选 vision/pose/训练环境补充可复现安装与权重版本/校验记录格式，实际可用平台单独验证，不自动下载未知来源模型。
- [x] 7.5 添加 CI（Node 22/Python 3.11/FFmpeg），执行 npm ci/build/test（2 workers）/lint、完整基础 pytest、OpenSpec 校验；固定工具版本且不依赖个人资产。
- [x] 7.6 验证从空模型目录启动基础运行时并停止，记录 PID/日志/失联语义和无残留进程的结果。

## 8. 第三阶段：前端加载与文档收口

- [x] 8.1 按路由和共享消费边界动态导入重型页面，排除 Library/shared 静态反向导入导致的首包回流。
- [x] 8.2 增加加载/失败恢复边界，回归深链刷新、return/search、浏览器前进后退和采集/报告状态。
- [x] 8.3 增加 landing 全部静态 JS gzip 合计预算检查（≤350 KiB）并接入 CI，记录动态 chunk/CSS，验证静态拆包不能绕过预算。
- [x] 8.4 对齐 README、backend README、技术成果说明与硬件演示标识，补充基础/模型安装启动和测试入口；保留真实错误不回退 demo 的回归。

## 9. 最终验收与交付

- [x] 9.1 在隔离旧数据副本上执行备份、schema 迁移、摄像头/复核导入、重复执行与回滚演练，输出冲突及 legacy_unbound 清单，不删除原文件。
- [x] 9.2 在干净环境执行最终 build/test/lint/pytest/OpenSpec/资源预算门禁，归档命令、版本、结果与未验证项。
- [x] 9.3 验证上传→播放→分析状态→候选复核→刷新重试的集成流程；真实模型/双摄验收有条件则单独执行，否则记录所缺条件与未验证边界。
- [x] 9.4 对照本变更及既有 projection-diagnostics 场景完成证据映射，确认任务清单与实际结果一致，再准备后续归档。
