## 1. 标注包领域模型与文件存储

- [x] 1.1 定义标注包、包版本、导入预览和导入审计的数据模型及 Alembic migration，关联 CaptureTake 并保存状态、版本、manifest、原始 JSON、语义快照和确认令牌。
- [x] 1.2 实现受限于项目数据目录的标注包存储服务，创建包目录、manifest、标签配置与视频软链接，并在软链接不可用时提供显式复制策略。
- [x] 1.3 实现视频 fingerprint、FPS、时长和主视频就绪检查，兼容单摄与双摄已合并主机位，拒绝不完整录制。
- [x] 1.4 将现有 Vidat 标签映射抽取为版本化共享模块，定义范围事件、点事件、回合结果和比分锚点的结构化 metadata schema。
- [x] 1.5 为标注包创建、刷新、视频校验和历史版本留存编写后端单元测试。

## 2. Vidat 导出与本地工作台

- [x] 2.1 重构 `export_to_vidat.py` 以使用标注包服务，输出 manifest 关联的 Vidat JSON 而非覆盖固定文件名。
- [x] 2.2 完善导出内容：将 set/game/rally/间歇范围、回合结果、备注和比分修正锚点稳定编码为 Vidat action 与结构化 metadata。
- [x] 2.3 完善 `vidat_workbench.py` 的 Vidat 发现、服务状态检测、包发布/链接和 URL 生成，避免修改任意本地路径或静默复制视频。
- [x] 2.4 提供创建/刷新标注包、读取包状态和取得 Vidat 启动 URL 的 REST API，并补充 API 测试。
- [x] 2.5 保留并测试 CLI 的列出 CaptureTake、导出包、仅导出和打开 Vidat 命令，确保与 API 共用格式校验。

## 3. 解析与导入预览

- [x] 3.1 实现 Vidat JSON 解析器，校验 manifest 身份、视频、FPS、标签 ID、动作时间范围、配对关系和层级范围约束。
- [x] 3.2 将有效 Vidat action 规范化为确定排序的 AnnotationOperation，并定义其到 CodingAction、回合结果和 `correct_score` 锚点的映射。
- [x] 3.3 实现差异引擎，识别新增、删除、移动、类别变更、回合胜者变更与比分锚点变更，并保留不可确认的阻塞错误。
- [x] 3.4 在隔离状态中重放候选动作，生成受影响比分、局/盘和最终胜者摘要，作为预览的一部分。
- [x] 3.5 改造 `import_from_vidat.py --preview` 复用解析和差异引擎；将 `--apply` 改为必须提供有效确认令牌。
- [x] 3.6 为格式失败、FPS 不匹配、边界冲突、回合胜者变化和比分锚点生成完整的单元与 API 测试。

## 4. 确认导入与比赛投影重建

- [x] 4.1 实现创建带过期时间和内容哈希的导入预览/确认令牌 API，禁止过期、重放或内容不匹配的确认请求。
- [x] 4.2 实现确认事务：保存原始 JSON、导入审计和规范化动作快照，仅替换同一标注包来源的历史导入动作，并对其他人工来源报告冲突。
- [x] 4.3 扩展 CodingAction、TimelineEvent 和相关 schema 的来源/包版本溯源字段，并保证历史数据兼容读取。
- [x] 4.4 实现从确认动作序列重放的比赛状态和 TimelineEvent/CaptureSegment 投影重建，复用保存的 `scoring_ruleset_version` 与 `correct_score` reducer 语义。
- [x] 4.5 在重建前后校验片段层级、时间范围、计分状态和最终结果；失败时整笔事务回滚且保留预览供诊断。
- [x] 4.6 为确认成功、确认冲突、回滚、事件溯源、比分重算和片段重建编写集成测试。

## 5. 项目内操作界面

- [x] 5.1 在已完成录制任务和片段工作区增加 Vidat 标注包状态、导出/刷新和“在 Vidat 中编辑”操作，并对视频未就绪展示禁用原因。
- [x] 5.2 增加导入文件选择或已发布包选择入口，展示解析进度、阻塞错误和结构化差异摘要。
- [x] 5.3 增加确认导入界面，突出受影响的回合、比分、局/盘及最终胜者，使用确认令牌提交并在成功后刷新时间线/片段/比分状态。
- [x] 5.4 为前端操作状态、错误反馈、确认流程和成功刷新编写组件与行为测试。

## 6. 验证与文档

- [x] 6.1 使用一个真实单摄比赛视频执行导出、Vidat 编辑、导入预览、确认重建和分析片段创建的端到端验证。
- [x] 6.2 使用已合并双摄任务验证主机位标注包和未合并任务的阻止行为。
- [x] 6.3 验证原始 Vidat JSON、manifest 和语义快照可独立读取，并记录后续动作识别/检测/姿态训练数据转换入口。
- [x] 6.4 更新本地运行文档，说明 Vidat 安装发现、日常标注流程、失败恢复和训练数据工件位置。
