## 1. 高度契约与证据模型

- [x] 1.1 定义 segment/sample 高度有效性枚举、降级原因和接触高度来源常量，确保后端、artifact 与前端使用同一组值
- [x] 1.2 扩展事件锚点和重建内部模型，支持逐事件 `height_ft`、`height_source`、`height_confidence` 与 `height_uncertainty_ft`
- [x] 1.3 实现接触高度证据优先级：合格双摄事件证据 → 可验证图像/球员上下文 → 全局低可信先验，并执行配置范围限制
- [x] 1.4 为高度来源和无证据兜底行为补充单元测试，确认不按球场区域自动修改接触高度

## 2. 3D 重建地面约束

- [x] 2.1 为 `segment_reconstruction` 的 spline 控制点增加 `z >= 0` 优化硬边界，并保持 bounce 端 `z=0` 约束
- [x] 2.2 检查并同步 `bundle_refine` 等其他 3D 优化入口的高度下界，避免旁路仍产生负高度
- [x] 2.3 增加优化后密集采样校验：非有限高度、负高度、bounce 端偏离 0、段内穿地和异常高度跳变均可被识别
- [x] 2.4 将高度校验失败映射为结构化 3D 降级原因，禁止失败段以 `stereo_estimated_3d` 或可用 3D 状态发布
- [x] 2.5 为 3D 负高度、bounce 端和段内穿地增加回归测试，覆盖优化结果与发布状态两层

## 3. 2.5D 端点感知高度模型

- [x] 3.1 重写未知起点/未知终点的高度函数，禁止用已知端高度复制填充，确保仅终点锚点使用反向开放弧
- [x] 3.2 保证 hit→bounce、bounce→hit、hit→hit、bounce→loss 和 unknown→unknown 的端点高度与有效性语义分别正确
- [x] 3.3 将逐事件接触高度和来源接入 2.5D 重建，确保 hit→hit 不再无条件使用同一个固定高度
- [x] 3.4 让未知端的 `height_confidence` 按开放方向渐隐，同时保持末端坐标和事件语义不被伪造
- [x] 3.5 为单锚点恒高、未知起点、未知终点、bounce=0 和逐事件接触高度增加后端重建单元测试

## 4. 混合 artifact 与发布质量门

- [x] 4.1 扩展 v4 artifact 的 segment/sample 类型和序列化逻辑，保存高度有效性、来源、不确定度和失败原因
- [x] 4.2 在 hybrid segment builder 中区分合格 3D、非法 3D、可用 2.5D 和不可用段，非法 3D 优先回退同段 2.5D
- [x] 4.3 更新 composer/API 边界校验，禁止负/非有限高度段声明可用 3D，同时保持 v1/v2/v3/v4 历史 artifact 可读
- [x] 4.4 增加 artifact schema、历史兼容、不可变产物和负高度降级测试

## 5. 前端安全消费与可信渲染

- [x] 5.1 更新前端类型与 adapter，保留 `height_confidence`、`height_uncertainty_ft`、`height_source` 和高度有效性字段
- [x] 5.2 在 `BallTrajectoryScene` 几何构建前过滤负值、非有限值和无效高度，禁止通过裁剪负值伪装为地面轨迹
- [x] 5.3 根据高度置信度和来源弱化开放端高度信息，并在高度无效时断开对应 geometry 或使用合格 2.5D 降级结果
- [x] 5.4 增加前端 adapter、轨迹分段、负高度防御和历史产物读取测试

## 6. 真实素材验收与发布

- [x] 6.1 使用真实 `job-71166f62f7` v4 artifact 建立回归断言：正式渲染数据不得包含负高度，`flight-69` 不得继续以可用 3D 展示
- [x] 6.2 验证当前 60 秒素材的 2.5D 段中，未知端不再整段恒高，bounce 端严格落到地面
- [x] 6.3 验证有逐事件高度证据的击球点使用对应来源，无证据时明确保持低可信全局先验
- [x] 6.4 运行后端相关测试、前端相关测试、TypeScript 构建和 Vite 生产构建
- [x] 6.5 完成页面人工验收：45°、俯视、边线、底线和 45°底线视角均不显示地面以下轨迹，场外坐标仍按原有分类语义处理
