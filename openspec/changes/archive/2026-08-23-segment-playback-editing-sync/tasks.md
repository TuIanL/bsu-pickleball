## 1. 回放状态与播放器契约

- [x] 1.1 在 `SegmentManagerPage` 建立 `currentTimeMs`、`durationMs`、`activeSegmentId`、播放模式和拖拽保存状态，并统一封装片段有效起止时间计算。
- [x] 1.2 更新 `SegmentVideoPlayer` 的 imperative handle 和事件回调，区分 seek、普通播放与一次性片段播放，并在 `timeupdate`/`ended` 时可靠地自动暂停片段播放。
- [x] 1.3 为 `SegmentVideoPlayer` 增加片段播放起点、终点钳制、自然结束和时间更新回调测试。

## 2. 片段列表与页面联动

- [x] 2.1 将右侧片段普通点击改为定位并自动播放有效区间，维护 active/highlight 状态并在播放结束后保留最后片段高亮。
- [x] 2.2 拆分播放区域、复选框、标签编辑和管理按钮的事件边界，确保选择分析片段或双击编辑标签不会误触发播放。
- [x] 2.3 将播放器 `onTimeUpdate`、`onDurationReady` 接入页面，替换时间线固定的 `currentTimeMs={0}`，并让高亮随播放位置更新。
- [x] 2.4 更新 `SegmentManagerPage` 测试，覆盖点击播放、自动暂停、行高亮、时间线 seek、标签编辑和复选框互不干扰。

## 3. 时间线交互与拖拽草稿

- [x] 3.1 将 `EditableSegmentTimeline` 改为受控的当前时间/active 片段展示，支持点击片段块定位起点、点击空白定位任意时间，并突出当前播放块。
- [x] 3.2 将边界拖拽改为 pointer down/move/up 的本地草稿流程，拖拽移动只更新预览，释放时回调一次最终边界和原始 `edit_version`。
- [x] 3.3 增加拖拽边界的前端钳制、最小时长处理、离开时间线后结束拖拽、保存失败回滚和保存中禁用重复编辑。
- [x] 3.4 更新时间线测试，确认拖拽不会在 move 阶段调用 PATCH，且关键事件标记只读并保持原始位置。

## 4. 后端边界校验与并发保护

- [x] 4.1 在 `segment_edit_service.patch_segment` 抽取统一边界校验，拒绝负数、结束早于开始、超过可用媒体范围和低于最小片段时长的修正。
- [x] 4.2 保证边界校验失败时不递增 `edit_version`、不写入编辑操作，并让路由返回稳定的中文 400；保留版本冲突的 409 语义。
- [x] 4.3 确认 PATCH 只修改 `CaptureSegment`/编辑操作记录，不触碰 `SessionTimelineEvent`，并让后续分析继续使用有效边界。
- [x] 4.4 增加后端测试，覆盖合法修正、无效边界、版本冲突、媒体范围和关键事件快照不变。

## 5. 集成验收与回归

- [x] 5.1 运行片段管理相关前端单元测试、后端 segment editing/API 测试和 lint/typecheck，修复回归问题。
- [x] 5.2 在真实片段页验收“点击某一分 → 自动播放 → 到尾暂停 → 列表/时间线高亮与播放头同步”。
- [x] 5.3 在真实片段页验收时间线拖拽只在释放时保存 corrected 边界，并确认关键事件内容未改变、异常边界无法再次保存。
- [x] 5.4 更新片段管理交互说明，明确“时间线点击是 seek，边界拖拽修改片段有效范围，不修改关键事件”。
