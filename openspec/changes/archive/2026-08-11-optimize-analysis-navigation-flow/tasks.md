## 1. 导航上下文与路由基础

- [x] 1.1 在 `navigationTypes` 中定义任务来源枚举、双摄 session 上下文和可选的 replace 导航选项。
- [x] 1.2 扩展路由解析，使 `/analysis/tasks` 读取并校验 `source`、`session` 查询参数，非法来源安全回退到上传任务。
- [x] 1.3 增加任务列表上下文路径 helper，统一生成上传、普通录制和双摄录制任务列表 URL。
- [x] 1.4 扩展应用导航函数以支持 tab 状态的 replace 历史语义，同时保留页面跳转的 push 语义。

## 2. 任务管理与双摄卡片接入

- [x] 2.1 将 `AnalysisTasksPage` 的来源 tab 初始化和切换接入 URL 上下文，切换 tab 时保留 session 并避免重置到上传任务。
- [x] 2.2 为双摄录制卡片的创建、重试、Parent 任务详情、报告和录制工作台入口附加双摄来源及 session 上下文。
- [x] 2.3 为普通录制任务入口附加普通录制来源及 session/camera slot 上下文，保持单摄工程入口语义不变。
- [x] 2.4 在双摄任务列表返回后使用 session id 提供原录制卡片的稳定定位能力，并处理 session 不存在或列表异步加载的情况。

## 3. 双摄向导与标定状态

- [x] 3.1 为 `MultiViewAnalysisSetupPage` 增加统一顶部返回入口，所有退出和加载错误分支返回双摄任务管理上下文而非 `/capture`。
- [x] 3.2 为步骤 1、步骤 2 和步骤 3 增加上一步操作；确保上一步只改变向导步骤，不离开当前页面。
- [x] 3.3 保存 A/B 标定点位草稿和 calibration id，返回前置步骤时恢复草稿，重新完成标定时覆盖旧结果。
- [x] 3.4 扩展 `CourtCornerCalibrator` 支持可选初始点位，并区分“上一步”与“退出向导”的回调语义。
- [x] 3.5 处理向导提交失败：同步素材校验失败回到素材检查，普通提交失败停留确认页并允许重试，保留已有标定结果。
- [x] 3.6 检查每个步骤的禁用条件、重复提交保护和完成后导航，确保未完成 A/B 标定时无法启动 Parent 任务。

## 4. 详情、结果与录制工作台返回路径

- [x] 4.1 为 `AnalysisJobPage` 增加左上角统一返回按钮，并按显式来源、multiview Parent 推断、默认上传的优先级生成目标。
- [x] 4.2 让 `AnalysisDetailsPage`、`VisionPage`、`ReportPage` 的任务管理入口保留来源上下文，同时不破坏报告页返回视觉分析的层级关系。
- [x] 4.3 让 `RecordingWorkspacePage` 的返回按钮恢复进入时的任务来源和双摄 session；修正双摄任务中的“返回录制”文案和目标一致性。
- [x] 4.4 审核加载失败、任务不存在和报告未生成等错误状态，确保它们也提供稳定且来源一致的返回入口。

## 5. 按钮样式与组件一致性

- [x] 5.1 将双摄向导和标定组件中的 `primary-button`、`sport-button` 替换为已有的 `green-button`、`quiet-button` 或补充经过审计的兼容样式。
- [x] 5.2 全局检索受影响页面的未定义按钮 class，确保主要、次要、返回、focus 和 disabled 状态均有明确视觉反馈。

## 6. 测试与验收

- [x] 6.1 增加路由 query 参数、非法来源回退和 replace/push 导航语义测试。
- [x] 6.2 增加任务管理来源 tab 恢复、双摄卡片入口上下文和返回地址测试。
- [x] 6.3 增加双摄向导步骤回退、退出目标、标定草稿恢复、提交失败重试和按钮 disabled 状态测试。
- [x] 6.4 增加分析详情、视觉分析、报告、录制工作台在双摄来源下的返回路径回归测试，并覆盖普通录制/上传回退。
- [x] 6.5 运行相关 Vitest、TypeScript build、ESLint 和 OpenSpec 校验；必要时在浏览器中验证桌面与窄屏下按钮和返回入口不重叠。
