## 1. 状态模型与规则版本

- [x] 1.1 为 LiveCodingState 模型、前后端 schema 和 TypeScript 类型增加双方胜局、计分阶段、发球站位及比赛状态字段，并提供兼容旧记录的默认值
- [x] 1.2 创建数据库迁移，为新增状态字段建立非破坏性列和默认值，不重算历史 CaptureTake
- [x] 1.3 将新单打和双打 match CaptureTake 初始化为 `hybrid_21_best_of_5_v1`，同时保留旧 `side_out_singles_v1` 和 `manual` 版本读取路径

## 2. 统一计分 FSM

- [x] 2.1 扩展纯计分 reducer 状态和返回结果，使其表达比分、发球权、计分阶段、发球站位、胜局与局/场结束信号
- [x] 2.2 实现 0:0 至进入 20:20 前的每球得分逻辑，确保 rally 胜方得分并取得下一球发球权
- [x] 2.3 实现 20:20 后的发球得分逻辑与 21 分封顶，确保接发方获胜只交换发球权、发球方得分自动赢局
- [x] 2.4 实现按发球方本队比分奇偶派生站位：奇数左区、偶数（含 0）右区
- [x] 2.5 增加规则版本路由，使在线执行和重放按 CaptureTake 的 `scoring_ruleset_version` 选择一致的 reducer
- [x] 2.6 为普通比分、20:20 边界、站位奇偶、重打和旧规则兼容补充纯 reducer 单元测试

## 3. Coding Action 与比赛生命周期

- [x] 3.1 强制新规则的 `start_game` 提供合法 `initial_server_team`，缺失时不创建 segment、事件或 revision 变更
- [x] 3.2 更新 rally result 事务，在一次提交中关闭 rally、运行 FSM、更新 recent results，并在制胜分时自动关闭 game、写入最终比分和累计胜局
- [x] 3.3 在任一方取得第三局胜利时自动完成比赛并关闭内部 set，拒绝继续开局、开分或提交结果
- [x] 3.4 为新规则拒绝无胜负的 `end_rally`，同时保留历史规则和非新规则路径的兼容行为
- [x] 3.5 更新比分修正逻辑，校验 0 至 21 的比分并重新派生计分阶段、站位、局与比赛投影
- [x] 3.6 更新 undo/rebuild，使撤销普通结果或制胜分后正确恢复 segment、比分、发球状态、胜局和比赛状态
- [x] 3.7 补充 coding action API 测试，覆盖非法状态转移、单次开局、自动收局、三胜完赛、比分修正、撤销制胜分及 revision 幂等性

## 4. 前端比赛状态 View Model

- [x] 4.1 新建或抽取纯比赛操作 view-model，根据权威 live state 和 open segment 派生等待开局、等待开分、回合进行中及比赛完成状态
- [x] 4.2 将局开始改为先打开 A/B 先发选择器、选择后只提交一次 `start_game`，并支持无副作用取消
- [x] 4.3 重构 EventActionToolbar：等待开分只显示“开始第 N 分”，回合中显示等宽 A/B 胜与次级重打，移除独立“分结束”和用户可见“盘开始”主操作
- [x] 4.4 将换边、战术暂停、重点标记和撤销整理为次级操作，并在 pending 时禁用冲突操作且不伪造计分状态
- [x] 4.5 更新键盘快捷键，使开局键打开先发选择器，开分和结果键只在对应状态有效，弹窗打开时不提交后台 action
- [x] 4.6 为状态 view-model、局开始单次提交、按钮可用性、pending 防重和动态快捷键补充前端行为测试

## 5. 计分展示与单/双摄一致性

- [x] 5.1 更新 ScoreBoard，展示第 N 局、双方胜局、当前比分、发球方、每球/发球得分阶段及比赛完成结果
- [x] 5.2 根据 match format 和 `serving_side`，在单打显示左/右区发球，在双打显示左/右区队员发球
- [x] 5.3 更新 CompactScoreStrip，使双摄模式展示完整关键计分状态并接入与单摄相同的比赛操作 view-model
- [x] 5.4 移除双打手工计分占位和对双打结果按钮的过滤，确保单打、双打共用权威自动计分响应
- [x] 5.5 更新比赛准备态规则摘要和最近结果展示，保持 A/B 颜色、重打与进行中标记一致
- [x] 5.6 补充 ScoreBoard、CompactScoreStrip 和单/双摄工作台组件测试，覆盖 20:20 阶段、站位、胜局及完赛状态

## 6. 集成验证

- [x] 6.1 运行后端计分 FSM、coding action、迁移和 timeline 相关测试并修复回归
- [x] 6.2 运行前端类型检查、计分组件与 CaptureConsolePage 行为测试并修复回归
- [x] 6.3 分别验证一场单打和一场双打完整流程：选择先发、每球得分、20:20 发球得分、21 分自动收局、三胜自动完赛及撤销恢复
- [x] 6.4 验证历史 `side_out_singles_v1` 和 `manual` CaptureTake 仍能加载与重放，且不会被新规则重算
- [x] 6.5 在桌面与移动宽度检查单摄/双摄比赛操作区，确认按钮文字、计分信息和弹窗无重叠或布局跳动
